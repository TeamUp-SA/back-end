version: "3.8"

services:
  # ------------------- Database -------------------
  db:
    image: postgres:15
    container_name: teamup_postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - teamup
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - teamup

  # ------------------- User Service -------------------
  user-service:
    build: ./user-service
    image: user-service:latest
    container_name: user-service
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      DATABASE_DSN: ${DATABASE_DSN}
      JWT_SECRET: ${JWT_SECRET}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      S3_REGION: ${S3_REGION}
      USER_GRPC_ADDR: ${USER_GRPC_ADDR}
      S3_BUCKET_NAME: ${S3_BUCKET_NAME}
    ports:
      - "8081:8081"
    networks:
      - teamup

  # ------------------- Auth Service -------------------
  auth-service:
    build:
      context: .
      dockerfile: auth-service/Dockerfile
    image: auth-service:latest
    container_name: auth-service
    depends_on:
      - user-service
      - redis
    environment:
      DATABASE_DSN: ${DATABASE_DSN}
      JWT_SECRET: ${JWT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      OAUTH_REDIRECT_URL: ${OAUTH_REDIRECT_URL}
      USER_SERVICE_ADDR: ${USER_SERVICE_ADDR}
    ports:
      - "8082:8082"
    networks:
      - teamup

  # ------------------- KrakenD API Gateway -------------------
  krakend:
    image: krakend:latest
    container_name: krakend
    depends_on:
      - user-service
      - auth-service
      - app-service
    volumes:
      - ./krakend.json:/etc/krakend/krakend.json
    command: ["run", "-c", "/etc/krakend/krakend.json"]
    ports:
      - "8888:8888"
    networks:
      - teamup

  # ------------------- Zookeeper -------------------
  zookeeper:
    image: zookeeper:3.9
    container_name: zookeeper
    restart: always
    ports:
      - "2181:2181"
    networks:
      - teamup
    healthcheck:
      test: ["CMD-SHELL", "echo ruok | nc localhost 2181 | grep imok"]
      interval: 5s
      retries: 5
      timeout: 5s

  # ------------------- Kafka -------------------
  kafka:
    image: wurstmeister/kafka
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_ADVERTISED_HOST_NAME: kafka
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
    depends_on:
      zookeeper:
        condition: service_healthy
    networks:
      - teamup

  # ------------------- Notification Service -------------------
  notification-service:
    build: ./notification-service
    container_name: notification-service
    environment:
      MAILERSEND_SMTP_USER: ${MAILERSEND_SMTP_USER}
      MAILERSEND_SMTP_PASS: ${MAILERSEND_SMTP_PASS}
      MAILERSEND_SMTP_SERVER: ${MAILERSEND_SMTP_SERVER}
      MAILERSEND_SMTP_PORT: ${MAILERSEND_SMTP_PORT}
      MAILERSEND_API_KEY: ${MAILERSEND_API_KEY}
      MAILERSEND_FROM_EMAIL: ${MAILERSEND_FROM_EMAIL}
    depends_on:
      - kafka
    ports:
      - "8083:8083"
    expose:
      - 9999
    networks:
      - teamup

  # ------------------- TeamUp Application Service -------------------
  app-service:
    build: ./app-service
    image: app-service:latest
    container_name: app-service
    restart: always
    environment:
      APP_PORT: ${APP_PORT}
      APP_ENV: ${APP_ENV}
      MONGODB_URL: ${MONGODB_URL}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      S3_REGION: ${S3_REGION}
      S3_BUCKET_NAME: ${S3_BUCKET_NAME}
    ports:
      - "3001:3001"
    networks:
      - teamup
    depends_on:
      - kafka
      - notification-service

  # ------------------- Search Service -------------------
  search-service:
    build: ./search-service
    image: search-service:latest
    container_name: search-service
    restart: always
    environment:
      SEARCH_SERVICE_PORT: ${SEARCH_SERVICE_PORT:-4000}
      APP_ENV: ${APP_ENV}
      GROUP_SERVICE_URL: ${GROUP_SERVICE_URL:-http://app-service:3001}
      GROUP_SERVICE_TIMEOUT_SECONDS: ${GROUP_SERVICE_TIMEOUT_SECONDS:-5}
      ENABLE_GRAPHQL_PLAYGROUND: ${ENABLE_GRAPHQL_PLAYGROUND:-true}
      DEFAULT_SEARCH_LIMIT: ${DEFAULT_SEARCH_LIMIT:-20}
      MAX_SEARCH_LIMIT: ${MAX_SEARCH_LIMIT:-100}
    ports:
      - "${SEARCH_SERVICE_PORT:-4000}:${SEARCH_SERVICE_PORT:-4000}"
    networks:
      - teamup
    depends_on:
      - app-service

volumes:
  pgdata:
  redis-data:

networks:
  teamup:
    driver: bridge
